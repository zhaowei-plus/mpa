let a = 'hello';
let b = ' word';
// concat 连接两个字符串,返回一个新字符串
console.log(a.concat(b)); // hello word

console.log(a.indexOf('l')); // 2

console.log(a.charAt(2)); // l

console.log(a.lastIndexOf('l')); // 3

// match
// replace
// search
// slice
// split
// length
// toLowerCase
// toUpperCase

// substring substr都是获取字串，参数不一样
console.log(a.substring(1, 3)); // el

console.log(a.substr(1, 3)); // ell